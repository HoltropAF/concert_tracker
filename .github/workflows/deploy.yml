name: Deploy to Vercel
on:
  push:
    branches: [main]
  workflow_dispatch:
jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger Vercel deploy
        run: curl --fail --show-error --silent --request POST https://api.vercel.com/v1/integrations/deploy/prj_C1IVi7ZEq7xF1LafFRuPDdQNta3w/rJPkVXheRc
